# Лабораторная работа №4 — Статистика числового набора (Вариант 3)

## Описание:   
Программа вычисляет статистические показатели для набора целых чисел, переданных через аргументы командной строки.

## Формат вызова: 
./stats <метрика> [числа...]

## Метрики:
- `min` — минимальное значение
- `max` — максимальное значение
- `sum` — сумма всех чисел
- `avg` — среднее арифметическое (целое, округлённое вниз)

---

## Структура итогового проекта:

```
lab4/solutions/student-19
├── libft/
│ ├── inc/
│ │ └── libft.h
│ ├── src/
│ │ ├── ft_strlen.c
│ │ ├── ft_atoi.c
│ │ └── ... и т.д.
│ └── Makefile
├── inc/
│ └── libft.h
├── src/
│ └── main.c
├── Makefile
└── README.md
```
## Реализовано:
1. Таблица структур, связывающих имя метрики (`min`, `max`, `sum`, `avg`) с указателем на функцию
2. Функции обработки массива чисел (`ft_min_arr`, `ft_max_arr`, `ft_sum_arr`, `ft_avg_arr`)
3. Используются только функции из собственной библиотеки `libft`
4. В `main`:
   - обработка `argc/argv`
   - проверка корректности аргументов
   - преобразование строк в числа через `ft_atoi`
   - динамическое выделение памяти под массив чисел
5. Обработка ошибок:
   - недостаточное количество аргументов
   - неизвестная метрика
6. Указатели на функции:
   - выбор метрики реализован через таблицу переходов (`struct + function pointer`)
7. Вариативная функция:
   - реализована функция `compute`, демонстрирующая использование `va_list`
8. Освобождение памяти (`free`) и отсутствие утечек


## Сборка:

```bash
cd d/project/c_6310_labs/lab4/solutions/student-19

```

```bash
make

```


## Проверка:

1. Минимум

```bash
./stats min 5 3 8 1 9

```
Результат:

```
1

```

2. Максимум

```bash
./stats max 5 3 8 1 9

```
Результат:

```
9
```

3. Сумма 

```bash
./stats sum 5 3 8 1 9

```
Результат:

```
26
```

4. Среднее значение

```bash
./stats avg 5 3 8 1 9

```
Результат:

```
5
```
6. Проверка ошибок

###  Отсутствие аргументов

```bash
./stats

```
Результат:

```
Error: not enough arguments

```

### Неизвестная метрика

```bash
./stats hello 1 2 3

```

Результат:

```
Error: unknown metric

```